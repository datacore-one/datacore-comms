# Python dependencies for the comms module.
#
# Generated 2026-09-23 from the packages this module actually imports (AST, not
# grep), floored at the version it was running against. Floors rather than pins:
# these modules share one interpreter with 37 others, and exact pins across all
# of them guarantee a conflict nobody can resolve.
#
# An undeclared dependency does not fail where it is missing — it fails where
# nobody is looking. research's nightly run died at its import line every night
# while nightshift's worked, because the package had been installed by hand on
# one host and declared nowhere.
#
#   python3 -m pip install -r requirements.txt


Pillow>=12.0.0
PyYAML>=6.0.3
browser-cookie3>=0.20.1
google-api-python-client>=2.187.0
playwright>=1.58.0
python-dotenv>=1.1.1
requests>=2.32.5
tweepy>=4.16.0
